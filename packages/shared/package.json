{
  "name": "@locator/shared",
  "version": "2.0.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsc --watch --declaration --outDir dist --preserveWatchOutput",
    "build": "node ../../scripts/clean-package-dist.js && tsc -p tsconfig.build.json --declaration --outDir dist",
    "//build-cjs": "tsc --module commonjs --outDir dist/cjs",
    "ts": "tsc --noEmit",
    "test": "vitest run",
    "test:dev": "vitest watch",
    "lint": "eslint src --ext .ts,.tsx",
    "prepack": "pnpm build"
  },
  "devDependencies": {
    "@locator/dev-config": "workspace:^",
    "@types/semver": "^7.7.1",
    "jsdom": "^20.0.3",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  },
  "gitHead": "64d49c931e745b61713e3349b9f872c115632c94",
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "semver": "^7.7.4"
  }
}
